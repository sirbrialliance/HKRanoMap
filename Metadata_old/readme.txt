This is from the old method where we'd parse the decompiled Unity level files.

The new method just grabs the data by loading the scenes in the game. (See MappingCamera.cs)

This can be deleted once all the relevant functionality and data is migrated.
